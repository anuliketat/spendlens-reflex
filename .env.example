# SpendLens Environment Configuration

# Hugging Face Token (used for Fin-R1 model)
HF_TOKEN=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

# Gmail API Configuration
# Follow these steps to get the credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project
# 3. Enable the Gmail API
# 4. Create OAuth 2.0 credentials (Desktop application)
# 5. Download the credentials JSON and rename to credentials.json in project root
# 
# The credentials.json file will be created automatically in the project root
# on first run, and you'll be prompted to authorize Gmail access.

# Optional: Database configuration
# DATABASE_URL=sqlite:///spendlens.db

# Optional: Model configuration
# MODEL_NAME=mlabonne/Fin-R1
# QUANTIZATION_BITS=4
